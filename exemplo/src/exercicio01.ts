class ContaBancaria {
    private let saldo:number;

    depositar(valor:number):void{

    }

    sacar(valor:number):void{

    }
}

class Cliente {
    private const nome:string;
    private const cpf:string;
    private const nasc:Date;
    private const nomeMae:string;
    private const conta:ContaBancaria;

    constructor(nome:string, cpf:string, nasc:Date, nomeMae:string, conta:ContaBancaria){
        
    }
}